const AppConfig = {
  ChatSDK: {
    liveChatConfig: {
      log: false // Log the live chat configuration on init
    },
    onNewMessage: {
      log: true
    }
  }
}

export default AppConfig;